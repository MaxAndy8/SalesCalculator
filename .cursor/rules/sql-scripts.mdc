---
description: Конвенції для SQL-скриптів у sql/
globs: ["sql/**/*.sql", "src/**/*.cpp", "src/**/*.h"]
alwaysApply: false
---

# SQL-скрипти

- Скрипти виконуються по черзі (01_, 02_, …). Не змінювати порядок нумерації без потреби.
- Стиль: PostgreSQL; імена таблиць і колонок — snake_case.
